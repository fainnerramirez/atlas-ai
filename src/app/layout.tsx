import { Metadata } from "next";
import RootLayout from "./rootLayout";

export const metadata: Metadata = {
  title: "Atlas AI",
  description: "App para viajar por todo el mundo con la ayuda de la IA",
};

export default RootLayout;